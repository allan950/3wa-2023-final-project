{% extends 'base.html.twig' %}

{% block title %}Anime{% endblock %}

{% block body %}
    <h1>Anime</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anime.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ anime.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_anime_index') }}">back to list</a>

    <a href="{{ path('app_admin_anime_edit', {'id': anime.id}) }}">edit</a>

    {{ include('back/admin_anime/_delete_form.html.twig') }}
{% endblock %}
