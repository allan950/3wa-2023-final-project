{% extends 'base.html.twig' %}

{% block title %}Anime{% endblock %}

{% block body %}
<div class="admin-page">


{{ include('back/index.html.twig') }}

    <div class="content">

    <div>

    <header>
        <a href="{{ path('app_admin_anime_index') }}" class="back-btn">
            <i class="fi fi-rs-arrow-left"></i> back to list</a>
        
        <h3>Anime</h3>
    </header>

    <table class="table show">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anime.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ anime.label }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('back/admin_anime/_delete_form.html.twig') }}

    </div>
    </div>
    </div>
{% endblock %}
